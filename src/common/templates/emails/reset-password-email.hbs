<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Password Reset</title>
  <style>
    :root {
      --primary-color: #4F46E5;
      --primary-dark: #3730A3;
      --text-primary: #1F2937;
      --text-secondary: #6B7280;
      --background: #F9FAFB;
      --card-bg: #FFFFFF;
      --border-radius: 10px;
      --shadow: 0 10px 25px rgba(0, 0, 0, 0.05);
    }
    
    body {
      margin: 0;
      padding: 0;
      background-color: var(--background);
      font-family: 'Segoe UI', Arial, sans-serif;
      color: var(--text-primary);
      -webkit-font-smoothing: antialiased;
    }
    
    .email-wrapper {
      width: 100%;
      padding: 40px 15px;
      background-color: var(--background);
    }
    
    .email-content {
      max-width: 600px;
      margin: 0 auto;
      background-color: var(--card-bg);
      padding: 40px 30px;
      border-radius: var(--border-radius);
      box-shadow: var(--shadow);
    }
    
    .header {
      text-align: center;
      margin-bottom: 30px;
    }
    
    .logo {
      max-width: 140px;
      margin-bottom: 20px;
    }
    
    h1 {
      color: var(--primary-color);
      font-size: 24px;
      margin: 0;
      font-weight: 700;
    }
    
    h2 {
      font-size: 20px;
      font-weight: 600;
      margin: 24px 0 16px;
      color: var(--text-primary);
    }
    
    p {
      font-size: 16px;
      line-height: 1.6;
      margin: 16px 0;
      color: var(--text-secondary);
    }
    
    .emphasis {
      color: var(--text-primary);
      font-weight: 500;
    }
    
    .button-container {
      text-align: center;
      margin: 30px 0;
    }
    
    .button {
      display: inline-block;
      padding: 14px 32px;
      background-color: var(--primary-color);
      color: white;
      text-decoration: none;
      border-radius: var(--border-radius);
      font-size: 16px;
      font-weight: 600;
      transition: background-color 0.2s;
    }
    
    .button:hover {
      background-color: var(--primary-dark);
    }
    
    .link {
      display: block;
      word-break: break-all;
      padding: 15px;
      background-color: var(--background);
      border-radius: 6px;
      font-size: 14px;
      color: var(--primary-color);
      margin: 20px 0;
    }
    
    .divider {
      height: 1px;
      background-color: #E5E7EB;
      margin: 30px 0;
    }
    
    .footer {
      margin-top: 30px;
      text-align: center;
      font-size: 13px;
      color: var(--text-secondary);
    }
    
    @media (max-width: 600px) {
      .email-content {
        padding: 30px 20px;
      }
    }
  </style>
</head>
<body>
  <div class="email-wrapper">
    <div class="email-content">
      <!-- Password Reset Template -->
      <div class="header">
        <img src="https://res.cloudinary.com/di5ey5axg/image/upload/v1742110768/shopify/xjxomejex3ffr2jnb50s.png" alt="DistLink Logo" class="logo">
        <h1>Reset Your Password</h1>
      </div>

      <p>Hello <span class="emphasis">{{name}}</span>,</p>

      <p>We received a request to reset your password for your DistLink account. If you didn't make this request, you can safely ignore this email.</p>

      <div class="button-container">
        <a href="{{resetLink}}" class="button">Reset Password</a>
      </div>

      <p>If the button doesn't work, you can copy and paste this link into your browser:</p>
      
      <div class="link">{{resetLink}}</div>

      <p>For security reasons, this link will expire in 1 hour.</p>

      <div class="divider"></div>

      <p>Best regards,<br><span class="emphasis">The DistLink Team</span></p>

      <div class="footer">
        <p>This is an automated message. Please do not reply to this email.</p>
        <p>&copy; 2025 DistLink. All rights reserved.</p>
      </div>
    </div>
  </div>
</body>
</html>